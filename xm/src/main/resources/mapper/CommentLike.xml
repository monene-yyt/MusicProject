<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.xm.mapper.CommentLikeMapper">
    <!-- 插入点赞记录 -->
    <insert id="insertLike" parameterType="com.example.xm.entity.CommentLike">
        INSERT INTO comment_like (comment_id, user_id, create_time)
        VALUES (#{commentId}, #{userId}, NOW())
    </insert>

    <!-- 判断用户是否已经点赞过 -->
    <select id="existsByCommentIdAndUserId" resultType="boolean">
        SELECT COUNT(1)
        FROM comment_like
        WHERE comment_id = #{commentId}
          AND user_id = #{userId}
    </select>

</mapper>