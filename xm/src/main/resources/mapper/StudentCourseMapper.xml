<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.xm.mapper.StudentCourseMapper">

    <insert id="insert" parameterType="com.example.xm.entity.StudentCourse">
        INSERT INTO student_course (student_id, course_id)
        VALUES (#{studentId}, #{courseId})
    </insert>

    <delete id="delete">
        DELETE FROM student_course WHERE student_id = #{studentId} AND course_id = #{courseId}
    </delete>

    <select id="selectByStudentId" resultType="com.example.xm.entity.StudentCourse">
        SELECT * FROM student_course WHERE student_id = #{studentId}
    </select>

    <select id="exists" resultType="int">
        SELECT COUNT(*) FROM student_course WHERE student_id = #{studentId} AND course_id = #{courseId}
    </select>
</mapper>
